# SAGEMRO 代码清理分析报告（只读分析）

> 分析时间：2026-07-24
> 分析范围：frontend / admin / worker / 根目录
> 约束：纯分析，未修改任何代码

---

## 一、可直接删除的文件（9 个，零风险）

### 根目录一次性脚本（8 个）

这些 .js 文件是一次性开发/截图/调试工具，包含硬编码测试账号密码（`13900008888 / 123456`），被 git 跟踪但与应用无关。`capture-screenshots.js` 是其他 capture-*.js 的超集，说明其余是迭代废弃版本。

| 文件 | 类型 | 行数 | 删除安全性 |
|------|------|------|-----------|
| `accept-and-price.js` | API 调试脚本 | — | ✅ 硬编码工单号，已完成使命 |
| `capture-chat.js` | Playwright 截图 | — | ✅ 功能被 capture-screenshots.js 覆盖 |
| `capture-fix.js` | Playwright 截图 | — | ✅ 同上 |
| `capture-payment.js` | Playwright 截图 | — | ✅ 同上 |
| `capture-remaining.js` | Playwright 截图 | — | ✅ 同上 |
| `capture-screenshots.js` | Playwright 截图 | — | ✅ 截图已生成 |
| `capture-wallet.js` | Playwright 截图 | — | ✅ 同上 |
| `debug-chat.js` | Playwright 调试 | — | ✅ 调试用，已无用 |

> ⚠️ 安全隐患：这些文件包含硬编码测试凭据，即使删除文件，git 历史仍保留。建议同时清理 git 历史。

### 废弃配置文件（1 个）

| 文件 | 说明 |
|------|------|
| `netlify.toml.deprecated` | DEPLOY.md 明确说"确认 Pages 稳定后可删除"，已完全迁移到 Cloudflare Pages |

### 建议移动（非删除）

| 文件 | 建议 |
|------|------|
| `Online Marketplace Onboarding Questionnaire (1).docx` | 移至 `docs/` 归档 |
| `Online Marketplace Onboarding Questionnaire (FILLED).docx` | 移至 `docs/` 归档 |

---

## 二、Worker 后端死代码（~177 行可直接删除）

### 2.1 死函数：已定义但永远不会被调用（3 个，~171 行）

路由已返回 410 (Gone)，但 handler 函数完整存在，搜索确认零调用。

| 函数 | 位置 | 行数 | 路由状态 |
|------|------|------|---------|
| `handleRegisterEngineer()` | `index.js:2391` | ~75 行 | 返回 410 |
| `handleGetEngineerWallet()` | `index.js:5942` | ~34 行 | 返回 410 |
| `handleWithdrawRequest()` | `index.js:5979` | ~62 行 | 返回 410 |

### 2.2 未使用的导入（6 个）

| 导入符号 | 行号 | 说明 |
|---------|------|------|
| `base64UrlEncode` | `index.js:13` | auth.js 内部自用，index.js 从未直接调用 |
| `base64UrlDecode` | `index.js:14` | 同上 |
| `ALLOWED_ATTACHMENT_TYPES` | `index.js:41` | 从未引用 |
| `MAX_ATTACHMENT_SIZE` | `index.js:42` | 从未引用 |
| `ALLOWED_CHAT_IMAGE_TYPES` | `index.js:46` | 从未引用 |
| `MAX_CHAT_IMAGE_SIZE` | `index.js:47` | 从未引用 |

### 2.3 测试代码暴露在生产路由中（~501 行）

| 函数 | 位置 | 行数 | 问题 |
|------|------|------|------|
| `handleInitTestData()` | `index.js:9258` | ~172 行 | 生产路由可调用，可初始化测试数据 |
| `handleTestFullPricingFlow()` | `index.js:9433` | ~258 行 | 生产路由可调用，动态建表 |
| `handleInitDb()` | `index.js:9187` | ~71 行 | 生产路由可调用 |

> ⚠️ 安全风险：这些端点在生产环境中暴露，虽然有 admin 鉴权，但属于不必要的攻击面。

---

## 三、Frontend 死代码

### 3.1 未使用依赖（1 个）

| 依赖 | 版本 | 说明 |
|------|------|------|
| `react-router-dom` | 7.18.0 | 已安装但整个 frontend/src/ 零 import，应用用 window.location.pathname 手动路由 |

### 3.2 孤立组件（7 个，从未被任何文件导入）

| 组件 | 行数 | 说明 |
|------|------|------|
| `components/common/Button.jsx` | 69 | 可复用按钮组件，但全项目用原生 `<button>` |
| `components/AI/AIToolsPanel.jsx` | 344 | AI 服务工具面板，唯一消费者是下面的 aiServiceTools.js |
| `components/Settings/SettingsModal.jsx` | 573 | 旧版设置弹窗，已被 CustomerHomeModal.jsx 替代 |
| `components/Sidebar/ToolBar.jsx` | 120 | 侧边栏工具栏，已被 Sidebar.jsx 内联实现替代 |
| `components/Device/DeviceForm.jsx` | 150 | 设备创建表单，孤立 |
| `components/WorkOrder/RatingModal.jsx` | 180 | 客户评价弹窗，孤立 |
| `components/WorkOrder/EngineerReviewModal.jsx` | 180 | 工程师评价弹窗，孤立，与 RatingModal 高度重复 |

**级联死代码**（删除上述组件后以下文件也变死代码）：

| 文件 | 说明 |
|------|------|
| `data/aiServiceTools.js` | 唯一消费者是孤立的 AIToolsPanel.jsx |
| `data/loginPresets.js` | 完全孤立，零 import |

### 3.3 死 CSS 文件

| 文件 | 说明 |
|------|------|
| `styles/tokens.css` | 设计令牌定义，零 import，应用用 index.css 内联变量替代 |

### 3.4 空目录

| 目录 | 说明 |
|------|------|
| `components/Upsell/` | 空目录，从未放入任何文件 |

### 3.5 未使用的 API 函数（10 个，services/api.js）

| 函数 | 行号 | 备注 |
|------|------|------|
| `getEngineerWallet` | 771 | 注释标注"已停用，服务端返回 410" |
| `applyWithdraw` | 785 | 注释标注"已停用，服务端返回 410" |
| `getCustomerReviews` | 1103 | 零调用者 |
| `getRecommendedEngineers` | 706 | 零调用者 |
| `getWorkOrderPayout` | 841 | 零调用者 |
| `getRepairRecord` | 1113 | 零调用者 |
| `getWorkOrderMaterialItems` | 456 | 零调用者 |
| `addWorkOrderMaterialItem` | 465 | 零调用者 |
| `uploadChatImage` | 314 | 零调用者 |
| `getConversations` | 354 | 零调用者 |

---

## 四、Admin 后台问题

### 4.1 错误静默吞掉（8 处）

`.catch(() => {})` 模式，用户看不到任何错误提示，线上问题无法及时发现。

| 文件 | 行号 |
|------|------|
| `pages/UsersPage.jsx` | 249 |
| `pages/RatingsPage.jsx` | 125, 293, 356 |
| `pages/LeadsPage.jsx` | 156 |
| `pages/WorkOrdersPage.jsx` | 559, 570, 811 |

### 4.2 window.prompt() 用于关键业务交互（6 处）

全部集中在 `WorkOrdersPage.jsx`，用于退回原因、结算金额、发票号等关键操作。

| 行号 | 用途 |
|------|------|
| 689 | 退回工单时输入原因 |
| 721 | 付款确认备注 |
| 768 | 输入结算金额 |
| 770 | 输入交易参考号 |
| 771 | 输入内部备注 |
| 832 | 输入发票号 |

### 4.3 超大文件

| 文件 | 行数 | 建议 |
|------|------|------|
| `pages/WorkOrdersPage.jsx` | 1,564 | 按功能拆分（列表/详情/定价/付款/发票） |
| `pages/MaterialsPage.jsx` | 1,081 | 按功能拆分（列表/详情/批量导入） |
| `pages/UsersPage.jsx` | 844 | 可拆分用户管理和用户详情 |

### 4.4 好消息

- 无空目录
- 无 TODO/FIXME 标记
- 依赖干净（仅 4 个运行时依赖，全部在用）
- 无孤立组件

---

## 五、LEGACY 代码（标记但仍在运行，需谨慎处理）

### 5.1 SERVICE_OS_LEGACY 引用（25 处）

| 代码 | 位置 | 状态 |
|------|------|------|
| `hashPasswordLegacy()` | `auth.js:30` | 仍用于已有用户密码验证 |
| `settleEngineerWallet()` | `index.js:4978` | **仍被调用**（line 5167），工单完成后触发 |
| `handleAcceptTicket()` | `index.js:5536` | **路由仍调用**（line 11632） |
| `handleRejectTicket()` | `index.js:5605` | **路由仍调用**（line 11635） |
| `include_pending_legacy` 逻辑 | `index.js:5311` | 旧 pending 队列查询，仍活跃 |
| `legacyStoredCode` hack | `index.js:2222` | 跨字段验证码存储兼容 |

> 这些代码虽然标记为 LEGACY，但仍在运行中。**不能直接删除**，需要先确认业务上是否还需要旧的钱包结算/自助接单功能，再做迁移和移除。

### 5.2 LEGACY 数据库表（3 张）

| 表名 | 创建于 migration | 使用情况 |
|------|-----------------|---------|
| `engineer_wallets` | 002, 022_cn | `settleEngineerWallet()` 仍写入 |
| `engineer_deposits` | 002, 022_cn | 仅 `handleWithdrawRequest` 引用（死代码） |
| `engineer_withdrawals` | — | 仅 `handleWithdrawRequest` 引用（死代码） |

> `deposits` 和 `withdrawals` 两张表的代码引用已全部是死代码，理论上表也可以清理，但需确认生产数据。

---

## 六、低效模式统计

### 6.1 Worker 重复样板代码

| 模式 | 出现次数 | 优化方案 |
|------|---------|---------|
| `return errorResponse(error.message, 500)` | 112 次 | 抽取 `withErrorHandler(handler)` 高阶函数 |
| `if (error instanceof GuardError) return ...` | 21 次 | 合并到上述包装器 |

> 保守估计，引入错误处理包装器可以减少 ~400 行重复代码。

### 6.2 Worker 中文函数名别名

```js
const checkPricing合理性 = checkPricingReasonableness; // line 10203
```
注释说"等 Task #12 统一重命名再去掉"，但仍未被清理。

### 6.3 Frontend 近似重复组件

| 组件对 | 说明 |
|--------|------|
| `SettingsModal.jsx` (573行，孤立) vs `CustomerHomeModal.jsx` (361行，活跃) | 旧版/新版设置弹窗 |
| `RatingModal.jsx` (孤立) vs `EngineerReviewModal.jsx` (孤立) | 评价弹窗，结构几乎相同，且都未使用 |

---

## 七、汇总

### 按优先级排序的清理清单

| 优先级 | 类别 | 数量 | 估计行数 | 风险 |
|--------|------|------|---------|------|
| **P0** | 根目录脚本（含硬编码凭据） | 8 个文件 | — | 安全风险 |
| **P0** | 测试端点暴露在生产路由 | 3 个函数 | ~501 行 | 安全风险 |
| **P1** | Worker 死函数（410 路由） | 3 个函数 | ~171 行 | 零风险删除 |
| **P1** | Worker 未使用导入 | 6 个 | 6 行 | 零风险删除 |
| **P1** | Frontend 孤立组件 | 7 个文件 | ~1,616 行 | 零风险删除 |
| **P1** | Frontend 未使用依赖 | 1 个 | — | 零风险移除 |
| **P1** | Frontend 死 CSS / 空目录 | 2 项 | — | 零风险删除 |
| **P1** | Frontend 未使用 API 函数 | 10 个 | ~120 行 | 低风险（确认无调用后删） |
| **P2** | 废弃配置文件 | 1 个 | — | 零风险 |
| **P2** | Admin 错误静默 | 8 处 | — | 需逐个加错误提示 |
| **P2** | Admin window.prompt | 6 处 | — | 需替换为 Modal 组件 |
| **P2** | Worker 重复 catch 块 | 112 处 | ~400 行 | 重构，中风险 |
| **P3** | LEGACY 代码 | 25 处 | ~800 行 | 需业务确认后迁移 |
| **P3** | LEGACY 数据库表 | 3 张 | — | 需确认生产数据后清理 |

### 量化估算

| 指标 | 数值 |
|------|------|
| 可直接删除的文件 | 9 个根目录 + 9 个 frontend = **18 个文件** |
| 可直接删除的代码行 | **~2,400 行**（死函数 + 孤立组件 + 未使用导入 + 死 API 函数） |
| 重构后可减少的代码行 | **~900 行**（错误包装器 + 移除测试端点） |
| 需业务确认后可清理的代码行 | **~800 行**（LEGACY 代码） |
| 总计可清理 | **~4,100 行**（占总量 ~5%） |

### 不需要清理的部分

- `.gitignore` 配置合理，无遗漏
- `.map` 文件全部在 gitignore 范围内，零仓库膨胀
- Admin 依赖干净（4 个运行时依赖全在用）
- Worker 零运行时依赖，极简
- Frontend/Admin/Worker 均无 TODO/FIXME 标记堆积
- 测试文件全部测试现存功能，无孤儿测试
- `.worktrees/` 占 2.2GB 但已 gitignore，如分支工作完成可本地清理
